# Ignore the Vagrant private state directory (contains local VM configuration, locks, and private SSH keys)
.vagrant/

# Ignore log files generated by Vagrant or providers
*.log
vagrant-*.log

# Ignore locally downloaded or packaged boxes
*.box
